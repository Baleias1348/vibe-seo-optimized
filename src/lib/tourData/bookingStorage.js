export const getStoredBookings = () => {
        console.warn("getStoredBookings from localStorage is deprecated. Use Supabase functions instead.");
        return [];
    };

    export const saveStoredBookings = (bookings) => {
        console.warn("saveStoredBookings to localStorage is deprecated. Use Supabase functions instead.");
    };