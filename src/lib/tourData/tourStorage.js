export const getStoredTours = () => {
        console.warn("getStoredTours from localStorage is deprecated. Use Supabase functions instead.");
        return [];
    };

    export const saveStoredTours = (tours) => {
        console.warn("saveStoredTours to localStorage is deprecated. Use Supabase functions instead.");
    };